openapi: "3.0.3"
info:
  title: Test
  version: "1.0"
paths:
  /pets:
    get:
      parameters:
        - name: type
          in: query
        - name: type
          in: query
      responses:
        "200":
          description: OK
