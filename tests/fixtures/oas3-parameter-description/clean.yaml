openapi: "3.0.3"
info:
  title: Test
  version: "1.0"
paths:
  /pets/{petId}:
    get:
      parameters:
        - name: petId
          in: path
          required: true
          description: The ID of the pet
      responses:
        "200":
          description: OK
